# ─── SmartAssets Mobile App Environment Template ────────────────────────────
# Replace with your computer's local IP address (find it by running `ipconfig` on Windows or `ifconfig` on Mac)
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:5000/api

